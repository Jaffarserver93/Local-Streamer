Fix an issue in my Node.js/TypeScript server where FFmpeg fails with exit code 8 ("Error splitting the argument list").

Issue Details:
- Location: The HLS streaming/transcoding logic (around `spawn("ffmpeg", ...)` or `spawn2("ffmpeg", ...)`).
- Error: [HLS Error] FFmpeg failed (exit 8): Error splitting the argument list: Option not found

Requirements to Fix:
1. Locate every `spawn("ffmpeg", [...])` array construction in the codebase (both source files and generated dist/index.m3u8).
2. Clean up the FFmpeg command line arguments array:
   - Ensure input seeking (`-ss <seekAt>`) is placed BEFORE the input file (`-i <videoPath>`), not after it.
   - Convert all non-string values (numbers, durations, ports) using `String(...)`.
   - Ensure path strings containing spaces are correctly passed as individual array elements without manual/escaped quote wrapping.
   - Append `.filter(Boolean)` to the argument array so no `undefined`, `null`, or empty string `""` items are passed into `spawn()`.

Example of the correct target structure:
```javascript
const seekArgs = seekAt > 0 ? ["-ss", String(seekAt)] : [];

const ffmpegArgs = [
  "-y",
  "-loglevel", "error",
  ...seekArgs,
  "-i", videoPath,
  "-c:v", "libx264",
  "-preset", "ultrafast",
  "-tune", "zerolatency",
  "-c:a", "aac",
  "-b:a", "128k",
  "-f", "hls",
  "-hls_time", String(HLS_SEGMENT_DURATION || 6),
  "-hls_list_size", "0",
  "-hls_segment_filename", path.join(dir, "segment_%03d.ts"),
  path.join(dir, "index.m3u8")
].filter(Boolean);
